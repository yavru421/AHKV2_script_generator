{
  "name": "generate_python_code",
  "description": "Generate a complete runnable Python script from a natural language prompt; always returns full code.",
  "parameters": {
    "type": "object",
    "properties": {
      "prompt": {"type": "string", "description": "Natural language description of the Python script to create."}
    },
    "required": ["prompt"],
    "additionalProperties": false
  }
}
